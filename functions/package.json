{
  "name": "localbizgh-functions",
  "description": "LocalBiz GH Firebase Cloud Functions",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.0.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.0.0"
  },
  "private": true
}
```

**Step 4** — Scroll down → click **"Commit changes"** → click **"Commit changes"** again

Then check:
```
https://github.com/Amin4u2/localbizgh/actions
